%div.page-top-info
  %h3 プロフィールの編集

%div
  = form_for(@user) do |f|
    = f.hidden_field :student_id, value: @user.student_id

    %fieldset.form-group
      = f.label :name, '名前'
      = f.text_field :name
    %fieldset.form-group
      = f.label :password, 'パスワード'
      = f.password_field :password
      %small.text-muted 変更しない場合は空欄のまま「更新」をクリックしてください
    %fieldset.form-group
      = f.label :password_confirmation, 'パスワードの確認'
      = f.password_field :password_confirmation
    %fieldset.form-group.col-xs-4.col-xs-offset-4.center
      = f.submit '更新', class: "btn btn-success"
